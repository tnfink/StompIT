name:           stomp-lib
version:        0.1
cabal-version: >= 1.2
build-type:     Simple

library
  hs-source-dirs:  src
  build-depends:   base >= 4, network >= 2.3.0.6, HUnit == 1.2.4.2
  ghc-options:     -Wall
  other-modules:   Stomp
